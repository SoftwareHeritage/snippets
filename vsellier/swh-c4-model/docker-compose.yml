services:
  structurizr:
    build:
      context: docker
      args:
        - UID=${UID}
        - GID=${GID}
    ports:
      - 8080:8080
    volumes:
      - ${PWD}:/usr/local/structurizr
    command: local
